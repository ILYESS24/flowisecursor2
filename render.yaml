# Render Configuration for Flowise

## 🚀 Déploiement Render - Configuration Optimisée

### Build Command
```bash
pnpm install && pnpm build
```

### Start Command
```bash
cd packages/server && pnpm start
```

### Environment Variables
- `NODE_ENV=production`
- `PORT=10000`
- `DATABASE_URL=${{postgres.DATABASE_URL}}`
- `FLOWISE_USERNAME=admin`
- `FLOWISE_PASSWORD=your_secure_password`
- `FLOWISE_SECRETKEY=your_secret_key`
- `APIKEY_PATH=/opt/render/project/src/packages/server/.flowise`

### Health Check
- Path: `/api/v1/ping`
- Timeout: 300 seconds

### Auto-Deploy
- Branch: `main`
- Auto-Deploy: Enabled
